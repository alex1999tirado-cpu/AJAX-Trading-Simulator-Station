# Documentation

This folder is reserved for project documentation and visual assets.

Current screenshot path:

```text
docs/screenshot.png
```

Use this path for the README preview image. A populated market-data screenshot can replace the current image later without changing the README.
