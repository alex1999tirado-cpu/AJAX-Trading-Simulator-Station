# Documentation

This folder is reserved for project documentation and visual assets.

Recommended screenshot path:

```text
docs/screenshot.png
```

When a polished application screenshot is available, place it at `docs/screenshot.png` so the main README can reference the same stable path without changing the repository structure.
